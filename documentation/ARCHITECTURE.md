# Moved

This document now lives at [`docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md).

It moved on 2026-09-01, when the project's documentation was renamed to `docs/`
to match the GitHub convention. This stub stays behind because the old path is
referenced from outside this repository, and a link that 404s is worse than a
redirect that costs one click.
