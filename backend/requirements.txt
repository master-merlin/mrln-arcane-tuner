accelerate==1.14.0
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
args==0.1.0
attrs==26.1.0
av==16.1.0
bitsandbytes==0.50.2
braceexpand==0.1.7
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
clint==0.5.1
colorama==0.4.6; sys_platform == 'win32'
coloredlogs==15.0.1
datasets==4.5.0
decord==0.6.0
diffusers==0.40.0
dill==0.4.0  # HELD with multiprocess below. Dependabot #18 proposed 0.4.1 and the install FAILS: multiprocess 0.70.19 declares dill>=0.4.1 while datasets 4.5.0 declares dill<0.4.1, so the pair cannot both move while datasets is pinned here. datasets 5.0.1 widens it to dill<0.4.2 — that bump is the unblocker, and it is a major, so it is its own decision.
duckduckgo_search==8.1.1
einops==0.8.2
fastapi==0.141.1
filelock==3.32.4
flatbuffers==25.12.19
frozenlist==1.8.0
fsspec==2025.10.0
ftfy==6.3.1
h11==0.16.0
hf-xet==1.6.0
hpsv2==1.2.0  # installed with --no-deps (see install-deps.sh): its dev deps leaked into its metadata as `pytest ==7.2.0` + `pytest-split ==0.8.0` — it never imports either and registers no pytest11 hook (test_dependency_metadata_contracts.py), and would otherwise abort the bulk resolve against our own pytest pin
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.29.0
humanfriendly==10.0
idna==3.19
ImageIO==2.37.4
imageio-ffmpeg==0.6.0
importlib_metadata==8.7.1
iniconfig==2.3.0
iopath==0.1.10
Jinja2==3.1.6
json5==0.15.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
lazy_loader==0.5
llvmlite==0.49.0
lxml==6.1.2
markdown-it-py==4.2.0
MarkupSafe==2.1.5
mdurl==0.1.2
mpmath==1.3.0  # HELD: sympy 1.14.0 declares mpmath<1.4. Dependabot #18 proposed 1.4.1 and the resolve fails.
multidict==6.7.1
multiprocess==0.70.18  # HELD with dill above: 0.70.19 declares dill>=0.4.1, which datasets==4.5.0 forbids.
networkx==3.6.1
ninja==1.13.2
numba==0.67.0
numpy==2.3.5  # HELD: sam3 0.1.4 declares numpy<2.4.0. sam3 is installed --no-deps, so pip will NOT stop a violation here -- crossing that ceiling silently is a masking-runtime gamble, not a resolve error. Moves when sam3 widens it.
nvidia-ml-py==13.610.43; sys_platform != 'darwin'
onnxruntime-gpu==1.29.0; sys_platform != 'darwin'
opencv-python-headless==5.0.0.93
optimum-quanto==0.2.7
packaging==26.3
pandas==3.0.5
peft==0.20.0
pillow==12.3.0
pillow-avif-plugin==1.6.0
platformdirs==4.11.5
pluggy==1.6.0
pooch==1.9.0
portalocker==4.3.0
primp==0.15.0
prodigy-plus-schedule-free==2.0.1
prodigyopt==1.1.2
propcache==0.5.2
protobuf==5.29.6
psutil==7.2.2
pyarrow==23.0.1
pycocotools==2.0.11
pydantic==2.13.5
pydantic_core==2.46.5  # HELD: pydantic 2.13.5 pins pydantic-core EXACTLY (==2.46.5). Dependabot #18 proposed 2.48.0 independently of pydantic and the resolve fails. This line only ever moves when pydantic does.
Pygments==2.21.0
PyMatting==1.1.15
pyreadline3==3.5.6; sys_platform == 'win32'
# pytest-split is deliberately ABSENT and must not be re-added: it had no callers
# here, and 0.8.0 caps at pytest<8, so installing it silently DOWNGRADES the pin
# below rather than failing. Pinned by test_pytest_split_stays_uninstalled. (hpsv2 also
# declares both pins — see its own line for why that one is inert.)
pytest==9.1.1
pytest-asyncio==1.4.0
python-dateutil==2.9.0.post0
python-multipart==0.0.32
pytorch_optimizer==3.10.1
pywin32==311; sys_platform == 'win32'
PyYAML==6.0.3
qwen-vl-utils==0.0.14
referencing==0.37.0
regex==2026.7.19
rembg==2.0.81
requests==2.34.2
rich==15.0.0
ruamel.yaml==0.19.1
rpds-py==0.30.0
ruff==0.16.5
safetensors==0.8.0
sam3==0.1.4  # installed with --no-deps (see install-deps.sh). 0.1.2's stale `huggingface-hub<1.0` ceiling is GONE upstream (0.1.4 declares `<2.0,>=0.30.0`), so that reason has expired; --no-deps stays only to keep sam3 out of the bulk resolve, and the price is that its live ceilings — numpy<2.4, tqdm<4.68, typing_extensions<4.16 — are enforced by nothing. Those three pins are held below for exactly that, and `_harness/tools/pin_coherence.py` is what checks them. Import verified by test_sam3_imports_cleanly_despite_declared_hub_pin
scenedetect==0.7.1  # installed with --no-deps to keep opencv-python-headless (not GUI opencv-python)
scikit-image==0.26.0
scipy==1.18.1
sentencepiece==0.2.2
setuptools==78.1.1
shellingham==1.5.4
six==1.17.0
sophia-optimizer==0.2.5
soundfile==0.14.0
starlette==1.6.0
structlog==25.5.0
sympy==1.14.0
tifffile==2026.8.23
timm==1.0.29
tokenizers==0.22.2  # HELD: transformers 5.14.1 declares tokenizers<=0.23.0, and 0.23.0 was never released as a stable wheel (PyPI has 0.23.0rc0 then 0.23.1), so 0.22.2 is the highest INSTALLABLE version transformers allows. Dependabot #18 proposed 0.23.1, which the resolve rejects. Moves when transformers does (ARCHITECTURE D8 — a validated release decision, not a sweep).
torch==2.12.1
torchao==0.17.0
torchaudio==2.11.0  # maintenance mode — no 2.12 wheel; installed --no-deps (declares torch==2.11.0)
torchvision==0.27.1
tqdm==4.67.1  # HELD with numpy above: sam3 0.1.4 declares tqdm<4.68.0.
transformers==5.14.1
triton-windows==3.7.1.post27; sys_platform == 'win32'
triton==3.7.1; sys_platform == 'linux'
typer==0.27.2  # transformers requires the full `typer`, not typer-slim; both provide an importable `typer` module so only one may be installed (see test_only_one_typer_distribution_is_installed)
typing-inspection==0.4.4
typing_extensions==4.15.0  # HELD with numpy above: sam3 0.1.4 declares typing_extensions<4.16.0.
tzdata==2025.3
urllib3==2.7.0
uvicorn==0.52.4
wcwidth==0.8.3
websockets==17.1
wheel==0.48.0
xxhash==3.6.0
yarl==1.24.5
zipp==3.23.0
spandrel==0.4.2
webdataset==1.0.2
